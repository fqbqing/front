/**
 * @file 
 * @author ()
 */

define(function (require) {

    var config = {};

    config.template = require('./detail.tpl.html');

	config.templateMainTarget = 'TPL_order_detail';

    config.domEvents = {
	};

    return config;

});

