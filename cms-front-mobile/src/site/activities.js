/**
 * @file 
 * @author ()
 */

define(function (require) {

    var config = {};

    config.view = require('./activitiesView');

    config.model = require('./activitiesModel');

    return config;

});
