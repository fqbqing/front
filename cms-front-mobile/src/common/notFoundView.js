/**
 * @file 
 * @author ()
 */

define(function (require) {

    var config = {};

    config.template = require('./notfound.tpl.html');

	config.templateMainTarget = 'TPL_common_notfound';

    config.domEvents = {
	};

    return config;

});

